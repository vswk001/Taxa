# Taxa v0.4.5

**[简体中文](#简体中文) | [English](#english)**

---

## 简体中文

界面细节补丁。

### 🐛 修复

- **深色主题下的白色标题栏**：窗口标题栏由系统绘制，之前不跟随应用主题——深色界面顶上顶着一条白边，非常刺眼。现在标题栏会与应用主题实时同步（切换主题或系统偏好变化时立即生效），即使你的主题选择与系统设置不同也能保持一致。

### 🔄 升级
- 应用内 设置 → 关于 → 检查更新（可先「查看更新内容」）

---

## English

A UI polish patch.

### 🐛 Fixed

- **White title bar in dark theme**: the native window title bar is painted by the OS and previously ignored the in-app theme, leaving a jarring white strip above dark windows. The bar now follows the app theme in real time (updating on theme switches and system-preference changes), staying consistent even when your choice differs from the OS setting.

### 🔄 Upgrading
- Settings → About → Check for Updates (preview What's New first if you like)

---

### 📦 安装 / Downloads

| 平台 / Platform | 资产 / Asset |
|---|---|
| Windows | `Taxa_0.4.5_x64-setup.exe` / `.msi` |
| macOS (Apple Silicon + Intel) | `Taxa_0.4.5_universal.dmg` |
| Linux | `.AppImage` / `.deb` / `.rpm` |
| MCP 服务器 / MCP server | `taxa-mcp-*` |
